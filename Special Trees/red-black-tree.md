<div dir="rtl">

# 📚 درخت قرمز–سیاه (Red-Black Tree)

درخت قرمز–سیاه یک **درخت جستجوی دودویی خودمتعادل** است که برای ذخیره و بازیابی سریع داده‌های مرتب استفاده می‌شود. هر گره علاوه بر مقدار داده، یک بیت رنگ دارد: 🔴 قرمز یا ⚫ سیاه. این ویژگی باعث می‌شود درخت تقریباً متعادل باقی بماند.

---

## 🌳 ویژگی‌ها

- **گره‌ها:** هر گره یا 🔴 قرمز است یا ⚫ سیاه.
- **ریشه:** همیشه ⚫ سیاه.
- **گره قرمز:** نمی‌تواند فرزند قرمز داشته باشد (دو گره قرمز پشت سر هم نداریم).
- **گره‌های سیاه:** تعداد گره‌های سیاه در تمام مسیرهای ریشه تا برگ یکسان است.
- **برگ‌ها:** همه برگ‌ها (NIL) ⚫ سیاه در نظر گرفته می‌شوند.

---

## 🔀 تفاوت با دیگر درخت‌ها

### 🟢 با درخت دودویی ساده (Binary Search Tree)

- ارتفاع در بدترین حالت:
  - BST: \(O(N)\)
  - Red-Black Tree: \(O(log N)\)
- پایدار و قابل پیش‌بینی در درج و حذف.

نکته: N در اینجا منظور از تعداد گره‌های درخت است.

### 🟣 با درخت AVL

- AVL تعادل بهتری دارد اما چرخش‌ها بیشتر است.
- برای برنامه‌هایی با درج و حذف مکرر: Red-Black Tree ترجیح داده می‌شود.
- برای برنامه‌هایی با جستجوهای بیشتر و درج یا حذف کمتر: AVL مناسب‌تر است.

---

## ⚡ عملیات اصلی

### ✏️ ۱. درج (Insertion)

۱. درج مثل BST انجام می‌شود.  
۲. گره جدید 🔴 قرمز است.  
۳. اصلاح قوانین در صورت نقض:

- **حالت ۱:**
  - عمو 🔴 قرمز
  - → والد و عمو ⚫ سیاه
  - → پدربزرگ 🔴 قرمز
  - → بررسی ادامه می‌یابد

- **حالت ۲:**
  - عمو ⚫ سیاه
  - → چرخش (Left یا Right)
  - → تغییر رنگ

### 🔍 ۲. جستجو (Searching)

- مشابه BST: از ریشه شروع، به شاخه چپ یا راست می‌رویم تا گره پیدا شود یا به NIL برسیم.

### ❌ ۳. حذف (Deletion)

۱. حذف مثل BST انجام می‌شود.  
۲. اگر گره حذف شده ⚫ سیاه باشد، وضعیت **Double-Black** رخ می‌دهد.  
۳. اصلاح بر اساس رنگ برادر:

- برادر 🔴 قرمز → چرخش و اصلاح رنگ
- برادر ⚫ سیاه:
  - اگر همه فرزندان سیاه باشند → رنگ برادر تغییر کرده و مشکل به سطح بالا منتقل می‌شود
  - اگر حداقل یک فرزند 🔴 قرمز باشد → چرخش و اصلاح رنگ

---

## 🔄 چرخش‌ها (Rotations)

### ⬅️ چرخش به چپ (Left Rotation)

- فرزند راست x جایگزین x می‌شود.
- مراحل:  
  ۱. x.right = y.left  
  ۲. y.parent = x.parent  
  ۳. اتصال y به والد  
  ۴. y.left = x  
  ۵. x.parent = y

### ➡️ چرخش به راست (Right Rotation)

- فرزند چپ x جایگزین x می‌شود.
- مراحل:  
  ۱. x.left = y.right  
  ۲. y.parent = x.parent  
  ۳. اتصال y به والد  
  ۴. y.right = x  
  ۵. x.parent = y

---

## ✅ مزایا (Advantages)

- زمان اجرای عملیات اصلی در بدترین حالت: \(O(log n)\)
- پیاده‌سازی ساده و حافظه مشابه BST
- مناسب برای ساختارهای داده با عملکرد بالا مثل `map`، `set`، `TreeMap`

## ⚠️ معایب (Disadvantages)

- قوانین درج و حذف پیچیده‌تر از AVL
- کمی سربار اضافی هنگام درج و حذف

---

## 💻 کاربردها (Applications)

- پایه و اساس `map` و `set` در C++، `TreeMap` در Java
- سیستم‌های عامل (زمان‌بندی پردازش‌ها، نگاشت حافظه مجازی)
- صف‌های اولویت‌دار و هش‌مپ‌های مرتب
- هر جایی که نیاز به **دسترسی سریع به داده‌های مرتب** و **عملکرد پایدار** باشد

</div>
